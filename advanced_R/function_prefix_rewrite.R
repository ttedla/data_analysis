
# -------------------------------------------------------------------------

          # Rewriting the function prefix

# -------------------------------------------------------------------------

x <- 2
y <- 3

# -------------------------------------------------------------------------

x+y
# is the same as 
`+`(x,y)

# -------------------------------------------------------------------------

df <- data.frame(seq(10), letters[1:10], LETTERS[1:10])
df
names(df) <- c("seq", "let", "LET")
# -------------------------------------------------------------------------
df

# -------------------------------------------------------------------------

#Is the same as 
df <- `names<-`(df, c("ID", "Sml", "CAP"))
df

# -------------------------------------------------------------------------

for(i in 1:10) print(i)
`for`(i, 1:10, print(i))

# -------------------------------------------------------------------------

add <- function(x,y) x+y
lapply(list(1:3, 4:5), add, 3)
list(1:3, 4:5)

# Or
lapply(list(1:3, 4,5), `+`, 3)

# -------------------------------------------------------------------------

